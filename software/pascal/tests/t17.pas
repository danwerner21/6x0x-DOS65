PROGRAM T17;
VAR R: INTEGER;

FUNCTION SQUARE(X: INTEGER): INTEGER;
BEGIN
  SQUARE := X * X
END;

FUNCTION ADD(A, B: INTEGER): INTEGER;
BEGIN
  ADD := A + B
END;

BEGIN
  R := SQUARE(7);
  WRITELN(R);
  R := ADD(10, 32);
  WRITELN(R);
  WRITELN(SQUARE(5))
END.

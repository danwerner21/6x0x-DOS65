PROGRAM T32;

FUNCTION TWICE(A: INTEGER): INTEGER;
BEGIN
  TWICE := A + A
END;

FUNCTION TRIPLE(X: INTEGER): INTEGER;
BEGIN
  TRIPLE := TWICE(X) + X
END;

BEGIN
  WRITELN(TRIPLE(7))
END.

PROGRAM T33;
VAR
  R, S: REAL;
  I: INTEGER;
BEGIN
  R := 1.25;
  S := 2;
  WRITELN(R);
  WRITELN(S);
  WRITELN(R + S);
  WRITELN(S - 0.50);
  WRITELN(R * 2);
  WRITELN(7 / 2);
  WRITELN(-R);
  I := R + 2.75;
  WRITELN(I)
END.

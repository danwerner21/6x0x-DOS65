PROGRAM T05;
VAR
  X : INTEGER;
BEGIN
  X := 10;
  IF X > 5 THEN
    WRITELN(1)
  ELSE
    WRITELN(2);
  IF X = 10 THEN
    WRITELN(3);
  IF X < 5 THEN
    WRITELN(4)
  ELSE
    WRITELN(5)
END.

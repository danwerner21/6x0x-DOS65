PROGRAM T19A;
VAR A, B: INTEGER;

PROCEDURE GETPAIR(VAR X, Y: INTEGER);
BEGIN
  WRITE('X = ');
  READLN(X);
  WRITE('Y = ');
  READLN(Y)
END;

BEGIN
  GETPAIR(A, B);
  WRITE('SUM = ');
  WRITELN(A + B)
END.
